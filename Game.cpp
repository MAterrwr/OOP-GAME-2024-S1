#include "Game.h"

void Game::play(Player& player, Monster& monster) {
    while (player.isAlive() && enemy.isAlive()) {
        //Player's turn
        if (!enemy.isAlive()) {
            break;
        }
    }
}
        